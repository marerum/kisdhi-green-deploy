# Frontend Environment Variables
# Copy this file to .env.local and fill in the values
# SECURITY WARNING: Never commit .env.local files to version control!

# API Configuration (REQUIRED)
# URL of the backend API server
NEXT_PUBLIC_API_URL=http://localhost:8000

# Development Configuration
NODE_ENV=development

# SECURITY NOTES:
# - Only variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - NEVER add sensitive data (API keys, database URLs, secrets) with NEXT_PUBLIC_ prefix
# - Use HTTPS URLs in production (https://your-api-domain.com)
# - The backend API handles all sensitive operations and credentials